#include "main.h"

/**
 * _abs - writes a function that computes the absolute value of an integer
 * @r: The integer
 *
 * Return: r
 */

int _abs(int r)

{
	if (r < 0)
		r = -(r); /*define r for -ve values*/


	return (r);
}
