#include <stdio.h>
/*
 * main- Entry point
 * print a-z
 * Return: 0 - always success
 */
void print_alphabet(void);
{
	int i;
{
	for (i = 97; i <= 122; i++);
	putchar(c[i]);
}
	putchar('\n');
	return (0);
}
